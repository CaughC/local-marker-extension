# データプライバシーとセキュリティについて

## APIキーの保存場所

本拡張機能で入力された **Gemini API Key** は、Chromeブラウザが提供する `chrome.storage.local` という機能を使用して保存されます。

### 具体的な保存先
データはプロジェクトフォルダ内（`.git` 管理下）ではなく、ブラウザの**ユーザープロファイルフォルダ内**にある暗号化されたデータベース（LevelDB形式）に保存されます。

**Windowsの場合の例:**
`C:\Users\<ユーザー名>\AppData\Local\Google\Chrome\User Data\Default\Local Extension Settings\<拡張機能ID>`

### セキュリティ上の特性

1.  **Gitリポジトリへの影響なし:**
    保存場所がプロジェクトフォルダとは完全に隔離されているため、`git add` や `git push` を行っても、**APIキーがリポジトリに含まれることはありません**。`.gitignore` への追加も不要です。

2.  **Webページからの隔離 (サンドボックス):**
    この保存領域は、閲覧しているWebサイト（悪意のあるサイトを含む）からはアクセスできません。この拡張機能自身からのみ読み書き可能な領域です。

## データの削除

この拡張機能をChromeから削除（アンインストール）すると、`chrome.storage.local` に保存されたデータ（APIキーを含む）も**同時に完全に削除されます**。

## 注意点

このデータは外部（クラウドや開発者）には送信されませんが、**お使いのPC自体を直接操作できる第三者**（またはマルウェア）が、上記のプロファイルフォルダ内のデータベースファイルを解析した場合は、キーを取り出せる可能性があります。
これはブラウザに保存したパスワードなどと同様のリスクレベルであり、PC自体のセキュリティ管理に依存します。
